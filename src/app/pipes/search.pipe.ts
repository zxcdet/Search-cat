import { Pipe, PipeTransform } from '@angular/core';import { BreedInterface } from '../types/search.interface';@Pipe({  name: "search",})export class SearchPipe implements PipeTransform {  transform(article: BreedInterface[], saerchStrings: string[], length: number): BreedInterface[] {    if(saerchStrings){      length = article.length      return article.filter(articleName => {        return saerchStrings?.some(substring=>articleName.description.includes(substring))          || saerchStrings?.some(substring=>articleName.name.includes(substring))          ;      });    }    else { return article }  };}